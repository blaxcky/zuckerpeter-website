<section class="features" id="features">
  <div class="features-grid animate-on-scroll">
    <article class="feature-item">
      <span class="feature-icon">🥐</span>
      <h3 class="feature-title">Hausgemacht</h3>
      <p class="feature-text">Mit Liebe von Hand gemacht.</p>
    </article>
    <article class="feature-item">
      <span class="feature-icon">🫙</span>
      <h3 class="feature-title">Hochwertige Zutaten</h3>
      <p class="feature-text">Ich verwende nur die besten Zutaten.</p>
    </article>
    <article class="feature-item">
      <span class="feature-icon">🍪</span>
      <h3 class="feature-title">Immer Frisch</h3>
      <p class="feature-text">Gebäck, das immer frisch und sicher.</p>
    </article>
  </div>
</section>

<style>
  .features {
    padding: 1rem 1.1rem 1.2rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.7rem;
    text-align: center;
  }

  .feature-item {
    min-width: 0;
    padding: 0.75rem 0.55rem 0.4rem;
  }

  .feature-icon {
    display: inline-grid;
    place-items: center;
    width: 2.35rem;
    height: 1.8rem;
    margin-bottom: 0.45rem;
    border-radius: 0.65rem;
    background: rgba(255, 246, 235, 0.8);
    font-size: 1.15rem;
    box-shadow: inset 0 0 0 1px rgba(171, 121, 72, 0.14);
  }

  .feature-title {
    font-size: 1.02rem;
    font-weight: 700;
    line-height: 1.15;
    color: var(--color-text);
  }

  .feature-text {
    margin-top: 0.3rem;
    font-size: 0.62rem;
    color: var(--color-text-muted);
    line-height: 1.42;
  }

  @media (min-width: 780px) {
    .features {
      padding: 1.5rem 3rem 1.75rem;
    }

    .features-grid {
      gap: 1.25rem;
    }

    .feature-item {
      padding: 1.1rem 1rem 0.85rem;
      background: rgba(255, 248, 239, 0.48);
      border-radius: 1rem;
      box-shadow: inset 0 0 0 1px rgba(151, 109, 73, 0.08);
    }

    .feature-icon {
      width: 3rem;
      height: 2.2rem;
      font-size: 1.35rem;
    }

    .feature-title {
      font-size: 1.3rem;
    }

    .feature-text {
      font-size: 0.82rem;
    }
  }
</style>
