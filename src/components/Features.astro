<section class="features" id="features">
  <div class="features-inner">
    <div class="features-header animate-on-scroll">
      <span class="features-label">Unsere Werte</span>
      <h2 class="features-heading">Was uns besonders macht</h2>
    </div>
    <div class="features-grid">
      <article class="feature-card animate-on-scroll" style="transition-delay: 0.1s">
        <span class="feature-number">01</span>
        <h3 class="feature-title">Hausgemacht</h3>
        <p class="feature-text">
          Von Hand gefertigt
        </p>
        <p class="feature-text">mit Hingabe und Sorgfalt</p>
      </article>
      <article class="feature-card animate-on-scroll" style="transition-delay: 0.2s">
        <span class="feature-number">02</span>
        <h3 class="feature-title">Hochwertige Zutaten</h3>
        <p class="feature-text">
          Regionale Zutaten
        </p>
        <p class="feature-text">sorgfältig ausgewählt</p>
      </article>
      <article class="feature-card animate-on-scroll" style="transition-delay: 0.3s">
        <span class="feature-number">03</span>
        <h3 class="feature-title">Immer Frisch</h3>
        <p class="feature-text">
          Frisch auf Bestellung
        </p>
        <p class="feature-text">für vollen Geschmack</p>
      </article>
    </div>
  </div>
</section>

<style>
  .features {
    padding: var(--space-2xl) var(--space-md);
    background: var(--color-bg-warm);
    position: relative;
  }

  .features::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-border), transparent);
  }

  .features::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--color-border), transparent);
  }

  .features-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .features-header {
    text-align: center;
    margin-bottom: var(--space-xl);
  }

  .features-label {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-sm);
  }

  .features-heading {
    font-size: clamp(2.2rem, 4vw, 3.5rem);
    font-weight: 400;
    color: var(--color-text);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .feature-card {
    background: var(--color-bg);
    border-radius: var(--radius);
    padding: 2rem 1.75rem;
    text-align: center;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    box-shadow: 0 1rem 2.75rem rgba(44, 24, 16, 0.05);
  }

  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(44, 24, 16, 0.08);
  }

  .feature-number {
    display: inline-block;
    margin-bottom: 1.25rem;
    color: var(--color-accent);
    font-family: var(--font-display);
    font-size: 2rem;
    line-height: 1;
  }

  .feature-title {
    font-size: 1.45rem;
    font-weight: 500;
    margin-bottom: 0.65rem;
    color: var(--color-text);
  }

  .feature-text {
    font-size: 0.96rem;
    font-weight: 300;
    color: var(--color-text-muted);
    line-height: 1.65;
  }

  .feature-text + .feature-text {
    margin-top: 0.1rem;
  }

  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
      max-width: 24rem;
      margin: 0 auto;
    }
  }
</style>
